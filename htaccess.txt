# Passenger Configuration
PassengerAppRoot "/home/c2527231c/backend.lipsempirebyarielle.store"
PassengerBaseURI "/"
PassengerPython "/home/c2527231c/virtualenv/backend.lipsempirebyarielle.store/3.10/bin/python"

# Enable Friendly Error Pages
PassengerFriendlyErrorPages on

# Serve Static Files
<IfModule mod_rewrite.c>
    RewriteEngine On
    # Avoid redirect loops for static and media files
    RewriteCond %{REQUEST_URI} ^/static/ [OR]
    RewriteCond %{REQUEST_URI} ^/media/
    RewriteRule ^ - [L]
</IfModule>